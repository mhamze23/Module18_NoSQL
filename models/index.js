// Import the Users and Thoughts models
const Users = require('./Users');
const Thoughts = require('./Thoughts');

// Export an object containing the Users and Thoughts models
module.exports = { Users, Thoughts };
